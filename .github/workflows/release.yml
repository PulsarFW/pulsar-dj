name: Create release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Bundle files
        run: |
          mkdir -p staging/pulsar-dj
          rsync -a --exclude='.github' --exclude='node_modules' --exclude='staging' . staging/pulsar-dj/
          cd staging && zip -r ../pulsar-dj.zip pulsar-dj/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          name: "PULSAR-DJ ${{ github.ref_name }}"
          body: |
            ![Pulsar Framework](https://r2.fivemanage.com/GPYOH8Hq4GPyAY7czrgLe/pulsarbanner.png)

            ## PULSAR-DJ ${{ github.ref_name }}

            In-world DJ booth — stream audio to nearby players for Pulsar Framework.

            ### Installation
            1. Download `pulsar-dj.zip` below
            2. Extract into your server's `resources` folder
            3. Add `ensure pulsar-dj` to your `server.cfg`

            ---
            ![Pulsar Framework](https://img.shields.io/badge/Pulsar-Framework-7c3aed?style=flat-square)
            ![Built for FiveM](https://img.shields.io/badge/Built_for-FiveM-F40552?style=flat-square)
          files: pulsar-dj.zip
          prerelease: false
